import { describe, test } from "vitest";

describe("Transfer Service", () => {
  test.todo("should transfer amount between two accounts");
  test.todo("should reject when balance is insufficient");
});